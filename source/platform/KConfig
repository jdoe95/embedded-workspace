# source/platform/KConfig
menu "Platform"
	config PLAT_NOCONTEND
		bool "Disable CONTEND assertion"

	config PLAT_USE_CONTEND_FAIL
		bool "Use contend_fail() even if assert() is available"
		depends on !PLAT_NOCONTEND
endmenu
